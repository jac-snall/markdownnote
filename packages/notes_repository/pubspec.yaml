name: notes_repository
description: A repository that handles note related requests
version: 0.0.1
homepage:
publish_to: none

environment:
  sdk: ">=2.16.2 <3.0.0"

dependencies:
  notes_api:
    path: ../notes_api

dev_dependencies:

